#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../js-yaml/bin/js-yaml.js" "$@"
else
  exec node  "$basedir/../../../js-yaml/bin/js-yaml.js" "$@"
fi
# cmd-shim-target=C:/Users/Aarav Aditya/Desktop/coffee/node_modules/.pnpm/dmg-builder@26.8.1_electron-builder-squirrel-windows@26.8.1/node_modules/js-yaml/bin/js-yaml.js
